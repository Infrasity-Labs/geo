# Copy to .env (or set repo secrets) for local dev/tests
# Supabase (server-side use only; do not expose service role key to browser)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_COMPANY_ID=
SUPABASE_LABEL=default

# Model provider auth (if you still run OpenRouter locally)
OPENROUTER_API_KEY=
