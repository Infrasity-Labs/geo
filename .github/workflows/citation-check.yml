name: citation-check

on:
  schedule:
    - cron: "0 0,8,16,20 * * *"
  workflow_dispatch:

jobs:
  run-citations:
    runs-on: ubuntu-latest
    env:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: python -m pip install --upgrade pip requests

      - name: Run gpt-oss-20b free online
        env:
          MODEL_SLUGS: openai/gpt-oss-20b:free:online
        run: python run.py

      - name: Run claude 3.5 haiku online
        env:
          MODEL_SLUGS: anthropic/claude-3.5-haiku:online
        run: python run.py

      - name: Run perplexity sonar online
        env:
          MODEL_SLUGS: perplexity/sonar:online
        run: python run.py

      - name: Commit and push logs
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add logs/*.json logs/*.jsonl logs/main_log.md || true
          if git diff --cached --quiet; then
            echo "No log changes to commit."
          else
            git commit -m "Update citation logs [skip ci]" || true
            git push origin HEAD:${GITHUB_REF#refs/heads/} || true
          fi
